import { Box } from "@mui/material";
import Layout from "./components/layout";

const App: React.FC = () => {
  return (
    <Box sx={{bgcolor:'#F5F5F8'}}>
      <Layout />
    </Box>
  );
};

export default App;
